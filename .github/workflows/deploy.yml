name: DEPRECATED - DO NOT USE
# This file is deprecated. We now use deploy-dev.yml and deploy-prod.yml.
# It is kept here empty to prevent it from triggering since we cannot easily delete via MCP.

on:
  workflow_dispatch: