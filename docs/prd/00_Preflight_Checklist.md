PRD-000: Interactive Environment Readiness Guide

Version: 1.5
Status: Active Guidance
Objective: Провести пользователя через процесс финальной настройки окружения (Local & Remote) до начала генерации кода и обеспечить решение технических блокировок.

1. Этап I: Синхронизация Контекста (Local)

Агент подтверждает, что видит текущее состояние и готов к работе:

Репозиторий: upgrade-crm-core инициализирован на GitHub.

Структура: Папки /backend, /frontend, /infra, /.antigravity и /docs созданы.

Индексация: Агент подтверждает, что прочитал PRD-001 — PRD-006 и 00_Legacy_System_Analysis.md.

Инструментарий: Агент проверяет работоспособность внутренних систем (Shell, Browser Subagent).

🛠 Разрешение технических блокировок (Blocker Resolution)

Если при инициализации возникают системные ошибки (например, failed to create browser context или $HOME variable not set), агент обязан:

Четко классифицировать ошибку (Системная / Конфигурационная).

Предоставить пользователю команду для исправления (например, установку зависимостей Playwright или экспорт переменной).

Предложить альтернативный путь (например, использование скриншотов из docs/references/ до починки браузера).

2. Этап II: Подготовка сервера (Remote Infrastructure)

Агент выступает в роли консультанта. Инструкция для агента:

Инициируй диалог: Спроси пользователя об IP-адресе нового сервера.

DNS: Проверь (через ping/nslookup), направлен ли домен devupgrade.space4you.ru на этот IP.

Доступ: Запроси SSH-ключ или пароль (пользователь готов предоставить их в рамках сессии).

Среда: Помоги пользователю проверить и, если нужно, установить:

Docker & Docker Compose.

Node.js & NPM.

Наличие прав в /opt/upgrade-crm/.

3. Этап III: Настройка секретов и CI/CD

Агент помогает связать GitHub и сервер:

Инструктаж: Четко скажи пользователю, какие именно секреты (Secrets) нужно добавить в настройки GitHub (HOST, SSH_PRIVATE_KEY, USERNAME).

Проверка: После добавления выполни тестовый "Handshake" (ssh -T).

Env-файлы: Подготовь .env.template на базе рекомендаций из PRD-006.

4. Этап IV: Доступ к Legacy System

Поскольку доступ к sales.upgradecrm.ru критичен для воссоздания интерфейса:

Запрос данных: Если креденшелы не найдены в защищенных переменных окружения, агент обязан запросить Логин и Пароль (или файл auth_cookies.json) напрямую у пользователя.

Авторизация: При успешном запуске браузера выполнить вход и подтвердить доступность страниц редактора.

Fallback: При невозможности запустить браузер (технический блокер), агент сообщает, что переходит на работу по скриншотам из docs/references/UI_MAP.md.

5. Этап V: Статус "Поехали!" (Handshake)

Агент завершает этап подготовки итоговым отчетом:

🚀 UPGRADE CRM: ENVIRONMENT STATUS

Локальный код: Структура проверена, ТЗ проиндексировано.

Сервер: Связь установлена (IP: [IP], Domain: devupgrade.space4you.ru).

Docker/Node: Версии проверены, среда готова.

Legacy: Статус доступа (Verified via Browser / Fallback to Images).

CI/CD: Секреты на месте, готовы к первому деплою.

Правило: Если что-то не работает, не просто сообщай об ошибке, а предлагай конкретный шаг для решения или запрашивай недостающие данные (пароли, ключи).


Дополнительно 
 # Точка входа
    base_url: "https://sales.upgradecrm.ru/dashboard/program/event/16#conference-id-92"
    
    # Креденшелы (В продакшне используй ENV переменные!)
    credentials:
      username: "vladislav.shirobokov@gmail.com"
      password: "vladislav456"
    
    # ИНСТРУКЦИИ ДЛЯ СУБАГЕНТА (СЦЕНАРИЙ)
    instructions: |
      GOAL: Inspect the UI layout and styles of the Program Editor.

      STEP 1: AUTHENTICATION (If redirected to Login Page)
      - Check if the current URL contains '/login' or if a login form is visible.
      - Action: Locate the input field for 'Email' or 'Username'. Type: "{{config.credentials.username}}".
      - Action: Locate the input field for 'Password'. Type: "{{config.credentials.password}}".
      - Action: Click the 'Login' / 'Sign In' button.
      - Wait: Ensure the page redirects to '/dashboard' or the target URL.

      STEP 2: NAVIGATION & INSPECTION
      - Navigate to: {{config.base_url}}
      - Wait for the grid element (or loading spinner) to resolve.
      
      STEP 3: ANALYSIS (READ-ONLY)
      - Inspect the 'Schedule Grid' container. Identify:
        * Is it CSS Grid or Flexbox?
        * What are the exact pixel widths of columns?
      - Open the 'Settings' modal (Gear icon) if possible to inspect form layouts.
      - Extract color codes (hex) for the Session status badges.

      CONSTRAINT: Do NOT delete or modify any business data. This is a production inspection only.