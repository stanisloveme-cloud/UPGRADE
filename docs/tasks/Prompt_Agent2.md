Ты — ИИ-агент (Antigravity), которому поручено выполнить срез задач в рамках проекта **"Anti-Gravity Rules"**.

### Твой контекст:
Мы разрабатываем CRM-систему для управления мероприятиями (Nest.js + React/Vite + PostgreSQL + Prisma).
Текущая задача описана в **PRD-11: Библиотека Брендов, Интеграция с Tilda и Улучшения UX**. Мы распараллеливаем работу, и ты — **Агент 2 (Frontend CRM)**.

### Твои первые шаги (ОБЯЗАТЕЛЬНО):
1. **Изучи правила:** Прочитай содержимое папки `Anti-Gravity Rules` (особенно файлы внутри папки `Anti-Gravity`), чтобы понять архитектуру и наши стандарты разработки.
2. **Используй MCP GitHub:** Подключи MCP-сервер `github`, чтобы проанализировать последние коммиты и PR, чтобы понять текущее состояние проекта на ветке `main`.
3. **Изучи ТЗ:** Внимательно прочитай твое персональное техническое задание по пути: `docs/tasks/PRD11_Agent2_Frontend.md`. В нем описано, что конкретно тебе нужно сделать (изменение UI раздела "Бренды", добавление компонента Upload для логотипов 10MB SVG/PNG, чекбоксы "Выгружать на сайт" и CSS-фильтры B&W для спикеров).
4. **Взаимодействие с Backend:** Бэкенд (схема БД и API) прямо сейчас разрабатывается параллельно другим агентом. Опирайся на обновленную структуру `prisma/schema.prisma` и используй заглушки (moks) в API-запросах, если реальные эндпоинты еще не готовы.
5. **Тестирование:** В проекте внедряется автотестирование. Убедись, что твои React-компоненты (особенно модальные окна редактирования и селекторы сегментов) покрыты базовыми тестами, чтобы мы были уверены, что UI не сломается.

**Начни свою работу с инициализации, чтения правил и изучения ТЗ по указанному пути. Сообщи мне, когда будешь готов приступить к коду!**
